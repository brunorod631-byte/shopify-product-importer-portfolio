# Contribuir

1. Usá fixtures offline y una tienda Shopify de prueba si necesitás integración.
2. Nunca agregues tokens, bases, logs o URLs privadas.
3. Ejecutá `pytest` y `python demo.py` antes de enviar cambios.
4. No habilites publicación automática en ejemplos.
